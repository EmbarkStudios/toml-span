[package]
name = "integ-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
basic-toml = "0.1"
codespan-reporting = "0.11"
insta = { version = "1.34", features = ["json"] }
pretty_assertions = "1.4"
toml-file = { workspace = true, features = ["reporting", "serde"] }
